## 1. 设置窗口布局

- [x] 1.1 将 `SettingsWindow.xaml` 的单页设置内容改为左侧 `TabControl`，建立“通用”“窗口位置”“硬件监控”“LED 灯效”“关于”五个标签页。
- [x] 1.2 按规范移动现有控件，保留全部 `x:Name`、事件绑定、输入控件文案和位置预设操作。
- [x] 1.3 将每个标签页的内容放入可滚动区域，并将“应用”“保存”和状态文本保留在标签页外的固定底部区域。
- [x] 1.4 调整窗口尺寸、标签头宽度和样式，使五个标签名称完整显示且最长内容可正常浏览。
- [x] 1.5 在“关于”标签页显示当前程序集版本、项目 Release 链接、作者 GitHub Profile、技术栈和开源组件信息。
- [x] 1.6 在“关于”标签页增加检查更新按钮和状态提示，保留链接点击后使用默认浏览器打开的行为。

## 2. 代码适配与保存行为

- [x] 2.1 检查并调整 `SettingsWindow.xaml.cs` 的初始化、保存和事件绑定，确保标签页重排不影响现有逻辑。
- [x] 2.2 确认跨标签页编辑后“应用”和“保存”仍读取并持久化所有配置字段，且不改变现有输入校验规则。
- [x] 2.3 确认位置预设的保存当前位置、跳转、删除、拖拽排序和托盘菜单刷新行为保持不变。
- [x] 2.4 使用 `HttpClient` 调用 GitHub latest Release API，设置 User-Agent 和超时，解析 `tag_name` 并比较版本；不新增第三方依赖。
- [x] 2.5 实现异步检查更新结果处理：新版本提示并打开 Release 页面，已是最新提示无需更新，失败提示无法检查；检查期间禁用按钮。

## 3. 验证

- [x] 3.1 构建 `MagicCenterHub` 项目，确认 XAML 编译、命名控件解析和事件绑定无错误。
- [ ] 3.2 验证打开设置窗口、切换五个标签页及底部操作区显示正常。
- [ ] 3.3 验证在多个标签页修改有效值后分别点击“应用”和“保存”，确认配置保存结果和窗口状态符合规范。
- [ ] 3.4 验证位置预设管理操作及 Esc 关闭行为未回归。
- [ ] 3.5 验证关于页版本、项目 Release 链接、作者主页、技术栈和开源组件信息显示正确，链接可由默认浏览器打开。
- [ ] 3.6 使用可控的当前版本和远程版本场景验证新版本、已是最新、版本解析失败及网络失败提示。
