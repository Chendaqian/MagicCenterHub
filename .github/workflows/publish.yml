name: Build and Publish

on:
  push:
    tags:
      - 'v*'  # 只在推送 v 开头的 tag 时触发
  workflow_dispatch:  # 支持手动触发

jobs:
  build:
    runs-on: windows-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '8.0.x'

      - name: Restore dependencies
        run: dotnet restore src/MagicCenterHub

      - name: Build
        run: dotnet build src/MagicCenterHub --configuration Release --no-restore

      - name: Publish
        run: dotnet publish src/MagicCenterHub --configuration Release --no-restore --output ./publish

      - name: Create Release
        if: startsWith(github.ref, 'refs/tags/v')
        uses: softprops/action-gh-release@v2
        with:
          files: |
            publish/MagicCenterHub.exe
            publish/MagicCenterHub.dll
            publish/Newtonsoft.Json.dll
            publish/System.Text.Encoding.CodePages.dll
            publish/Resources/AgentStatusLight.ico
            publish/Resources/AgentStatusLight.png
          generate_release_notes: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
